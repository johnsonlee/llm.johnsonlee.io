<svg xmlns="http://www.w3.org/2000/svg" width="1200" height="630" viewBox="0 0 1200 630">
  <defs>
    <linearGradient id="bg" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#111"/>
      <stop offset="100%" stop-color="#191919"/>
    </linearGradient>
    <linearGradient id="accent" x1="0" y1="0" x2="1" y2="0">
      <stop offset="0%" stop-color="#e7ad52"/>
      <stop offset="100%" stop-color="#f0c674"/>
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="1200" height="630" fill="url(#bg)"/>

  <!-- Decorative corner accents -->
  <path d="M0,0 L120,0 L0,120 Z" fill="#e7ad52" opacity="0.08"/>
  <path d="M1200,630 L1080,630 L1200,510 Z" fill="#e7ad52" opacity="0.08"/>

  <!-- Border -->
  <rect x="40" y="40" width="1120" height="550" rx="20" fill="none" stroke="#e7ad52" stroke-width="2" opacity="0.3"/>

  <!-- Top decorative bar -->
  <rect x="460" y="140" width="280" height="4" rx="2" fill="url(#accent)" opacity="0.6"/>

  <!-- Title -->
  <text x="600" y="230" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="80" font-weight="bold" fill="#e7ad52">LLM for Kids</text>

  <!-- Divider -->
  <rect x="480" y="260" width="240" height="3" rx="1.5" fill="#fff" opacity="0.2"/>

  <!-- Subtitle -->
  <text x="600" y="330" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="30" fill="rgba(255,255,255,0.85)">A visual guide to Large Language Models</text>

  <!-- Description -->
  <text x="600" y="400" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="22" fill="rgba(255,255,255,0.5)">From Transformers to GPT — explained with simple analogies and diagrams</text>

  <!-- Bottom decorative bar -->
  <rect x="460" y="450" width="280" height="4" rx="2" fill="url(#accent)" opacity="0.6"/>

  <!-- URL -->
  <text x="600" y="510" text-anchor="middle" font-family="system-ui, -apple-system, sans-serif" font-size="22" font-weight="bold" fill="#e7ad52" opacity="0.8">llm.johnsonlee.io</text>
</svg>
